name: Legacy V1 Release (quarantined)

on:
  workflow_dispatch:

jobs:
  quarantined-v1-release:
    runs-on: ubuntu-latest
    steps:
      - name: Explain quarantine
        run: |
          echo "V1 release is quarantined by LFC0-004."
          echo "No artifact is built or published from this workflow."
          echo "A supported local-first release path is deferred to LFC-9."
