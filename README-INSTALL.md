# Pipeline Kotlin V2 documentation package

Unzip this archive at the root of the `pipeline-kotlin` repository. It creates `docs/v2/` and does not overwrite the existing V1 documentation.

Recommended first read: `docs/v2/README.md`, then `docs/v2/DESIGN.md` and `docs/v2/05-roadmap/ROADMAP.md`.

The initial documentation commit should remain separate from code refactors so later ADR-to-code changes are traceable.
