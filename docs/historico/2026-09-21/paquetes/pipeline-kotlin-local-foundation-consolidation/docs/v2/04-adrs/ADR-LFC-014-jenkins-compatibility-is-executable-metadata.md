# ADR-LFC-014 — Jenkins compatibility is executable metadata

**Status:** proposed

## Context

Documentation-only compatibility claims drift from implementations.

## Decision

The familiarity catalogue becomes machine-readable input for generated docs/tests/migrations. F2/F3 require executable UAT fixtures.

## Consequences

Compatibility regressions fail CI and migration quality becomes measurable.
