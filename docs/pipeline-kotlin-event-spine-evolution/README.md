# pipeline-kotlin event spine evolution pack — ABSORBED (historical provenance)

This package was fully absorbed into the integrated documentation tree.
It is retained only as a historical provenance record and is NOT a
parallel specification set: it cannot authorize implementation, change a
gate, or supersede `docs/v2`.

The sole current authority is `docs/v2/` — see
`docs/v2/00-context/EVENT_SPINE_PACK_DISPOSITION.md` for the complete
absorption map, and `docs/v2/05-roadmap/EVENT_SPINE_EVOLUTION.md` for the
EVT/POL program.
