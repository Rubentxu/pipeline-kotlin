# E1.0 — Decision Receipt

**Checkpoint:** E1.0 (Inventory + scope + UAT)
**Cycle:** `cycle/e1-ecosystem-local-first`
**Closed at:** 2026-09-20T10:48Z
**Commits in this checkpoint:**
- `5124a6c8` E1.proposal (OpenSpec change artifacts + ledger)
- `f0c101fe` E1.proposal testing-state update
- `ff1c25f7` E1.0.T1 UAT plan (S1..S11)
- `d54c75ca` E1.0.T2 inventory snapshot (pre-cycle)

## Outcome

E1.0 closes with the **scope, UAT plan, inventory, and four
checkpoints frozen**. No code changes in this checkpoint.

## Frozen scope (from `proposal.md`)

Four checkpoints inside ONE cycle:

| # | Checkpoint | Output |
|---|---|---|
| E1.0 | Inventory + scope + UAT | This receipt + UAT plan + inventory |
| E1.1 | `core.junit` plugin | Plugin registry entry, codec, contract, handler, fixtures, contract suite |
| E1.2 | `core.artifact.query` bridge | Bridge to `core.archiveArtifacts`, query by name, derived index |
| E1.3 | UAT integral + closure | Receipt + roadmap pointer; tag `e1-ecosystem-local-first` |

## Frozen first increment (operator GO at 2026-09-20T08:44:00.852Z)

**α. `core.junit`** — read JUnit XML into a typed report.

Rationale (operator-confirmed):

- Closes the only missing piece of the local CI/CD loop.
- 0 hits in current catalog (`grep core.junit|junitReport|readJUnit`).
- Fits the SDK; no engine or coordinator special-case.
- Does not modify F1 or open F2.

## Frozen UAT plan (11 scenarios)

Source: `docs/v2/07-uat/E1_ECOSYSTEM_LOCAL_FIRST_UAT_PLAN.md`.

| ID | Title | Class | Evidence file |
|---|---|---|---|
| S1 | happy_junit_read | positive | S1-happy.txt |
| S2 | junit_test_failures_surface | negative | S2-fail.txt |
| S3 | junit_missing_file | negative | S3-missing.txt |
| S4 | junit_malformed_xml | negative | S4-malformed.txt |
| S5 | junit_unsupported_schema | negative | S5-unsupported.txt |
| S6 | artifact_query_happy | positive | S6-query-happy.txt |
| S7 | artifact_query_not_found | negative | S7-query-missing.txt |
| S8 | e2e_demo_happy | positive (E1.3) | S8-e2e-happy.txt |
| S9 | e2e_build_fail | negative (E1.3) | S9-build-fail.txt |
| S10 | e2e_missing_report | negative (E1.3) | S10-no-report.txt |
| S11 | e2e_resume | positive (E1.3) | S11-resume.txt |

11 scenarios. Cycle closes only when all 11 are green. Red on
S8..S11 blocks the cycle close.

## Frozen inventory snapshot (pre-cycle)

Source: `docs/v2/07-uat/E1_INVENTORY.md`.

Anchors:
- `CoreStepRegistryFactory.kt` sha256
  `bcd9d725a7207da35d2eef001d1b566982df36ed61ae0593cbee9e9e425d5023`.
- 16 Step registrations enumerated.
- 14 contract suite tests in `pipeline-application`.
- 30 compatibility corpus fixtures.
- 10 example pipelines.

Gap: `core.junit` (0 hits in catalog).

## Operator authorization table (frozen, verbatim from intake)

Authorized within cycle:
- Investigate inventory; pick an increment inside agreed scope.
- Write OpenSpec, ADR if needed, tasks, UAT.
- Implement plugins via existing SDK.
- Fix locally-reproduced defects that don't break contracts.
- Run tests, capture evidence, commit, advance to next checkpoint.
- Close cycle, integrate, archive via SDDK flow.

Requires operator GO (must STOP):
- Change a public certified semantics.
- Modify `sh` contract or open F2 without trigger.
- Add a plugin-specific exception to engine or coordinator.
- Introduce remote storage, new protocols, or incompatible public API.
- Alter historical receipts, replace a published release, delete foreign work.
- Continue if the integral goal is technically infeasible inside the limits above.

Out-of-cycle (no work):
- F2 (offset map in `Kotlin24ScriptingHost.mapDiagnostic`).
- Remote artifact storage (S3/GCS/OCI/Azure).
- Worker distribution / multi-node coordination.
- New public DSL surface incompatible with existing DSL contract.
- Re-implementing `core.archiveArtifacts` (already REGISTRY_PRIMARY).
- Modifying F1 (`sh` variable-scope contract).

## Cycle exit gate (frozen)

The cycle closes green when ALL hold:

- E1.0 closed (this receipt).
- E1.1 closed (checkpoint receipt + 5 contract test rows + 3 corpus fixtures).
- E1.2 closed (checkpoint receipt + 5 contract test rows + 2 corpus fixtures).
- E1.3 closed (closure receipt + 11/11 UAT scenarios green + L5 green + tag).
- F1 contract sha256 unchanged (`abc8f5bed09f...`).
- Roadmap pointer updated (`docs/v2/05-roadmap/LOCAL_PRODUCTION_READY_ROADMAP.md`).
- No operator GO escalation was triggered.

## Next

E1.1.T1..T10 — `core.junit` plugin implementation.

Cycle state: GREEN.
