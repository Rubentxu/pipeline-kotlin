# S2-B10 / G3 — `core.archiveArtifacts` StepContractSuite + Migration Readiness

**Cycle:** `lfc2-e1-s2-b10-core-archiveartifacts`
**Branch:** `cycle/lfc2-e1-archive-artifacts`
**Base:** `main @ be2f1aef` (S2-A10/G8 `core.cleanWs` CERTIFIED, counter `CERTIFIED Steps: 10`)
**HEAD at G3:** `2be125b8` (G2 differential contract freeze) + this slice's G3 commit
**Date:** 2026-09-13 (local +02:00)
**Status:** MIGRATION_READY — `AUTHORITY_FLIP_READY=true` (G4 executes only in the serial merge queue with explicit user GO)

## 1. Scope (slice S2-B10 / G3)

G3 adds the per-Step contract suite that certifies the G1 registry candidate
(`CoreArchiveArtifactsStep`) against the registry-driven open-world Step seam, and records
the delegated architecture-fitness evidence that G4 will re-verify against the flipped
authority.

Production changes at G3: **NONE**. G3 is test-only plus this receipt.

```text
new: v2/pipeline-application/src/test/kotlin/.../CoreArchiveArtifactsStepContractSuiteTest.kt
     25 tests (one per coverage row; archiveArtifacts carries three extra
     archiveArtifacts-specific rows: failure-variant codec, ArtifactArchived payload,
     G3 invariant)
new: docs/v2/07-uat/S2_B10_ARCHIVEARTIFACTS_G3_READINESS_RECEIPT.md (this file)
```

Production forbidden at G3 (per slice firewall — all honoured):

- no `LEGACY_PLUGIN_IDS` change (`core.archiveArtifacts` still in);
- no authority flip (the legacy `CanonicalArchiveArtifactsNodeDispatcher` remains the
  production authority for this key);
- no legacy removal (decoder branch / `ARCHIVE_ARTIFACTS_PLUGIN_ID` constant / `ArchiveArtifacts`
  subtype / metadata row / dispatcher file all still present);
- no `LegacyResidualSnapshot` edit (counters unchanged at 3 / 3 / 3).

## 2. G3 verification evidence (fresh XML + SHA-256 canary)

### 2.1 Contract suite — `:pipeline-application:test`

```text
canary   : TEST-dev.rubentxu.pipeline.v2.application.CoreArchiveArtifactsStepContractSuiteTest.xml
           deleted before the run, regenerated by the run (rule 25)
argv     : ./v2/gradlew -p v2 :pipeline-application:test
             --tests 'dev.rubentxu.pipeline.v2.application.CoreArchiveArtifactsStepContractSuiteTest'
exit     : 0
result   : tests="25" skipped="0" failures="0" errors="0"
timestamp: 2026-09-13T11:40:00.314Z
sha256   : 676528841223079503950e50faa82b6f6b28e1e7fccceb746e9a6e15067dae24
```

### 2.2 Architecture fitness — `:pipeline-architecture-tests:test` (R5, fresh)

```text
argv     : ./v2/gradlew -p v2 :pipeline-architecture-tests:test
canary   : test-results/test/ removed before the run, regenerated by the run
exit     : 1  (single pre-existing red: Lfc0GlobalStateFitnessTest — carried, NOT a regression)
```

| Suite | Result | Role for this slice |
|---|---|---|
| `Lfc2RegistryFamilyFitnessTest` | 3/0/0 | residual authority family boundary |
| `Lfc2DurableCoordinatorScopeFitnessTest` | 4/0/0 | coordinator scope/ownership; the registry seam does not widen coordinator coupling |
| `S3EchoLegacyRemovedFitnessTest` | 7/0/0 | retired-step absence |
| `S3EmitEventLegacyRemovedFitnessTest` | 8/0/0 | retired-step absence |
| `S3ErrorLegacyRemovedFitnessTest` | 12/0/0 | retired-step absence |
| `S3IsUnixLegacyRemovedFitnessTest` | 9/0/0 | retired-step absence |
| `S3PwdLegacyRemovedFitnessTest` | 8/0/0 | retired-step absence |
| `S3SleepLegacyRemovedFitnessTest` | 4/0/0 | retired-step absence |
| `S3WriteFileLegacyRemovedFitnessTest` | 4/0/0 | retired-step absence |
| `Lfc0GlobalStateFitnessTest` | **1 failure / 2** | **PRE-EXISTING red (carried, out of S2-B10 scope)** |

All `S3*LegacyRemovedFitnessTest` suites call `LegacyResidualSnapshot.assertConverged(root)`,
which reads the residual **live** from source (decoder `LEGACY_PLUGIN_IDS`, metadata rows,
`Canonical*NodeDispatcher.kt` files). Their green at G3 is therefore positive evidence that
the residual is exactly:

```text
LEGACY_PLUGIN_IDS = {core.load, core.waitUntil, core.archiveArtifacts}   size 3
metadata rows     = {core.load, core.waitUntil, core.archiveArtifacts}   size 3
dispatcher files  = {CanonicalLoadNodeDispatcher.kt,
                     CanonicalWaitUntilNodeDispatcher.kt,
                     CanonicalArchiveArtifactsNodeDispatcher.kt}        size 3
```

No `LegacyResidualSnapshot` edit was made or needed at G3.

### 2.3 L0 compile gate

```text
argv: ./v2/gradlew -p v2 :pipeline-application:compileTestKotlin
exit: 0  (BUILD SUCCESSFUL in 2s; 36 up-to-date, 1 executed)
```

### 2.4 Iteration receipts (rule 21 — RED must fail for the EXPECTED reason)

One RED iteration was consumed before green, recorded here for honesty (zero-fabrication):

```text
RED   test  : `typed failure — an empty match without allowEmptyArchive surfaces as the
              step's own SCRIPT failure` (line 641)
RED   reason: author assumption, NOT a product defect. EncodedOutput of a typed failure is
              NOT null — the failure variant of the output ADT is a legitimate durable
              value and IS persisted:
              {"kind":"archiveArtifacts","outcome":"FAILED","failureKind":"SCRIPT",
               "message":"archiveArtifacts: no files matched 'nope/*.jar'"}
              The assertion was corrected to decode-and-compare the failure variant.
              Contrast row 9a (thrown handler → ENGINE) where EncodedOutput IS null; that
              distinction is now pinned from both sides.
RED   count : 1 failure / 25 tests
GREEN count : 0 failure / 25 tests
```

A second, cosmetic iteration fixed a JUnit-5 `assertNotNull` return-type misuse (JUnit's
`assertNotNull` returns `Unit`, not the non-null value) — a compile error, not a RED.

## 3. Row-by-row coverage mapping (AGENTS.md §STEP IMPLEMENTATION golden path step 8)

| # | Row | Test method | Status |
|---|---|---|---|
| 1 | identity | `identity — KEY is core dot archiveArtifacts and duplicate registration fails` | ✅ |
| 2 | contract completeness | `contract completeness — key, descriptor, codecs, single capability, WRITES_WORKSPACE, MEMOIZED, None` | ✅ |
| 3 | input codec round-trip | `codec input — default input encodes to the canonical envelope and round-trips` | ✅ |
| 3a | input codec tolerant defaults | `codec input — decode accepts the legacy envelope without optional fields with tolerant defaults` | ✅ |
| 3b | input codec rejection | `codec input — decode rejects a foreign envelope kind` | ✅ |
| 4 | output codec round-trip (success) | `codec output — ArchiveArtifactsOutput round-trips byte-identically and survives durable string form` | ✅ |
| 4a | output codec round-trip (failure) | `codec output — ArchiveArtifactsFailureOutput round-trips with its SCRIPT kind and message` | ✅ |
| 4b | output codec rejection | `codec output — decode rejects a non-archiveArtifacts kind` | ✅ |
| 5 | canonical envelope | `canonical envelope — input codec is byte-identical to the legacy compiler lowering for non-default fields` (cross-accepts through `CanonicalCoreStepDecoder.decode`) | ✅ |
| 6 | production registry resolution | `registry resolution — production factory contains core dot archiveArtifacts` | ✅ |
| 6a | fresh factory consistency | `registry resolution — production factory registry is fresh per call and consistent across calls` | ✅ |
| 7 | capability declaration | `capability declaration — core dot archiveArtifacts declares EXACTLY ARTIFACT_ARCHIVE_OPERATIONS` (size 1) | ✅ |
| 7a | capability admission (available) | `capability admission — the capability available prepares Ready` | ✅ |
| 7b | missing capability | `missing capability — admission rejects and names the missing capability` | ✅ |
| 7c | conditional exposure | `conditional exposure — controlDirRoot null means the capability is absent and admission rejects` | ✅ |
| 8 | success | `success — boundary coexecute archives matched files into retention and returns typed success` (also asserts the `artefacts/<runId>/<stage>/...` retention path) | ✅ |
| 9 | typed failure (own SCRIPT kind) | `typed failure — an empty match without allowEmptyArchive surfaces as the step's own SCRIPT failure` | ✅ |
| 9a | thrown-handler negative control | `typed failure negative control — a thrown handler is re-classified ENGINE by the boundary` | ✅ |
| 10 | fresh durable | `fresh durable — first execution of core dot archiveArtifacts writes one terminal SUCCEEDED operation` | ✅ |
| 11 | replay (idempotent rerun) | `replay — MEMOIZED archiveArtifacts with WRITES_WORKSPACE reruns idempotently with identical entries` | ✅ |
| 11a | replay decision (policy unit) | `replay decision — DefaultEffectReplayPolicy reruns MEMOIZED WRITES_WORKSPACE with a SUCCEEDED entry` | ✅ |
| 12 | divergence | **COVERED BY G2** | ➡ G2 |
| 13 | observability | `observability — every core dot archiveArtifacts run emits a StepStarted StepFinished pair` | ✅ |
| 14 | architecture fitness | **DELEGATED** | ➡ §2.2 |
| 15 | real DSL scenario | `real DSL scenario — pipeline DSL lowers archiveArtifacts and the run succeeds on both authorities` | ✅ |
| 16 | `ArtifactArchived` payload | `ArtifactArchived payload — relPath sha256 size and archivedAt are present and self-consistent` | ✅ |
| 17 | G3 counters invariant | `G3 invariant — core dot archiveArtifacts is registry-primary-pending with counters 3 3 3` | ✅ |

### 3.1 Row 12 provenance (COVERED BY G2)

A same-input replay/divergence test **cannot** express the divergences this lane actually
freezes. The five frozen deltas (D1..D5) are authority-shape differences between the legacy
dispatcher and the registry candidate, including an **effect-classification divergence
(`{READ_ONLY}` → `{WRITES_WORKSPACE}`, D3)** which is a `StepDescriptor` property and is not
observable through the coordinator's replay surface at all. Forking a second divergence test
here would create a competing authority for the same facts.

Authority: `docs/v2/07-uat/S2_B10_ARCHIVEARTIFACTS_G2_DIFFERENTIAL_CONTRACT_FREEZE.md`
(195 lines) + `CoreArchiveArtifactsDifferentialContractTest.kt` — **10/0/0**, XML sha256
`5f6cae0f1353d59f26d87d8659e576433260b3ab40708c0fcf9d76b828039c8d`.

Per the `core.cleanWs` precedent (S2-A10), the differential suite is **deleted when
LEGACY_REMOVED closes the second leg**, with the deletion recorded in the G5 receipt.
Row 12 here is therefore a delegation to a suite that is contractually temporary.

### 3.2 Row 14 provenance (DELEGATED)

Architecture fitness is asserted by `:pipeline-architecture-tests:test`, not by a
per-Step contract suite. The per-Step suite cannot read the source tree that
`LegacyResidualSnapshot` reads, and reproducing those scans inside
`:pipeline-application` would duplicate the residual authority that
`LegacyResidualSnapshot` was created to centralise (its own KDoc records the
`core.pwd` hand-sync failure that motivated the consolidation).

Delegates, all green at G3 (§2.2): `Lfc2RegistryFamilyFitnessTest` (3/0/0),
`S3EchoLegacyRemovedFitnessTest` (7/0/0), `S3EmitEventLegacyRemovedFitnessTest` (8/0/0),
`S3ErrorLegacyRemovedFitnessTest` (12/0/0), `S3IsUnixLegacyRemovedFitnessTest` (9/0/0),
`S3PwdLegacyRemovedFitnessTest` (8/0/0), `S3SleepLegacyRemovedFitnessTest` (4/0/0),
`S3WriteFileLegacyRemovedFitnessTest` (4/0/0).

**G4 obligation:** after the authority flip, re-run this same set. G4 must also set
`LegacyResidualSnapshot.registryPrimaryPendingRemoval = "core.archiveArtifacts"` — that is
the ONE place the G4 transitional `2 / 3 / 3` state is declared.

## 4. G3 transitional honesty (mandatory disclosure)

`core.archiveArtifacts` is **still in `LEGACY_PLUGIN_IDS`** at G3. The production coordinator
therefore classifies the key `LegacyCore` and never reaches the G1 registry handler in a
production run. Asserting `StructuralFamily = Registry` here would be a **fabrication**.

The suite's `success` (8), `typed failure` (9), `replay` (11) and `ArtifactArchived payload` (16)
rows therefore drive the **exact seam G4 will promote** —
`RegistryExecutionPreparation.prepare` → `RegistryExecutionBoundary.coexecute` — which is the
registry-native path and is unaffected by `StructuralFamilyResolver` routing. This is the
same construction the certified `core.cleanWs` / `core.deleteDir` suites used.

Row 15 (`real DSL scenario`) uses the **production** wiring
(`CanonicalDurableRunCoordinator` + `CoreStepRegistryFactory.registry()`) and asserts on an
input on which **both** authorities agree by construction: `allowEmptyArchive = true` with a
non-matching pattern. Both the legacy and the registry authority succeed with an empty
`ArtifactArchived.files` list, so this row stays green **across the G4 flip** and does not
need editing at G4. The `node.pluginStepId == core.archiveArtifacts` assertion proves the DSL
lowering; the authority is deliberately NOT asserted here (row 17 pins the G3 state instead).

Row 17 pins the transitional invariant explicitly (`LEGACY_PLUGIN_IDS` size 3, key present,
metadata row present) so the G4 flip is forced to edit this row deliberately rather than
silently invalidate it.

## 5. Counters (post-G3, pre-G4)

```text
core.archiveArtifacts:
  REGISTERED         = true     (G1: CoreArchiveArtifactsStep.registerInto)
  REGISTRY_PRIMARY   = false    (LEGACY_PLUGIN_IDS still contains "core.archiveArtifacts";
                                 CanonicalArchiveArtifactsNodeDispatcher remains the
                                 production authority)
  LEGACY_UNREACHABLE = false
  LEGACY_REMOVED     = false

global residual:
  legacy ids       = 3   {core.load, core.waitUntil, core.archiveArtifacts}
  metadata rows    = 3
  dispatcher files = 3
  certified steps  = 10
  registry-primary = 11
```

G3 changes **zero** production semantics, so every counter is unchanged from the S2-A10/G8
close. This is the intended G3 property.

## 6. G4 exit criteria (carried forward, registered now)

G4 (`REGISTRY_PRIMARY`) MUST:

1. Remove `"core.archiveArtifacts"` from `CanonicalCoreStepDecoder.LEGACY_PLUGIN_IDS`
   → residual `3 / 3 / 3` → `2 / 3 / 3`.
2. Set `LegacyResidualSnapshot.registryPrimaryPendingRemoval = "core.archiveArtifacts"`
   (the ONE declaration site of the transitional state).
3. Update row 17 of this suite from the G3 `3 3 3` invariant to the G4 `2 / 3 / 3` invariant.
4. **Flip `CompatibilityCorpusTest.fixture10SmokeE2E` from `runFixtureFail` to
   `runFixturePass`.** This is a hard exit criterion, not an optional cleanup: G2 proved that
   the legacy `CanonicalArchiveArtifactsNodeDispatcher` anchors its glob against absolute
   paths (`workspaceRoot = controlDirRoot.resolve("workspace")`) so it can NEVER match, making
   `10-smoke-e2e.pipeline.kts` fail end-to-end (`exit=1`,
   `ArtifactArchiveFailed: No files matched glob pattern 'build/libs/*.jar'`). Frozen delta
   **D1** (glob engine → certified `AntStyleGlob`) turns that fixture green. Leaving that flip
   out of G4 would leave the corpus asserting the defect.
   Evidence: `docs/v2/07-uat/evidence/s2-b10-g2/fixture10-legacy-glob-defect.json`
   (sha256 `67ee075a...`).
5. Re-run §2.2 in full and record fresh XML.

## 7. Decision requested

`AUTHORITY_FLIP_READY = true`. G3 is complete and green.

**STOP** — awaiting explicit user **GO** for G4.
