# Proposed event contracts for real examples

These files are **schema proposals**, not production parser commitments. Their job is to make the expected
80/20 constraint language concrete before implementation.

The implementation should decode YAML/TOML into typed ADTs and reject unknown fields/unsupported
constraints fail-closed.
