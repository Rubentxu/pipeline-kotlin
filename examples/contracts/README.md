# Candidate normalized event contracts for existing real examples

Examples 07–10 already exist and are GREEN in the real CLI gate at `d0ccf4b5`. These files are **schema proposals**, not new example proposals and not production parser commitments. They normalize already-proven P4-EX laws so EVT-3 can differential-test a reusable harness against the existing `run.sh` oracle.

The implementation should decode YAML/TOML into typed ADTs and reject unknown fields/unsupported
constraints fail-closed.
