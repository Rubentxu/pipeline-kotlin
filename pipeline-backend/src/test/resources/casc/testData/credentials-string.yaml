credentials:
  system:
    localCredentials:
      credentials:
        - string:
            scope: GLOBAL
            id: "secret-text"
            secret: "${SECRET_TEXT}"
            description: "Secret Text"
