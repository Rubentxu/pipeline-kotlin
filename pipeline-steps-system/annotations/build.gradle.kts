plugins {
    kotlin("jvm")
}

// Módulo ligero con solo las anotaciones
// No debe tener dependencias pesadas para poder incluirse fácilmente en proyectos de usuarios
dependencies {
    // Solo kotlin-stdlib, nada más
}